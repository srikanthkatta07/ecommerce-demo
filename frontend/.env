VITE_API_URL=http://localhost:8080
VITE_S3_BUCKET_NAME=springbootecommercedemo
VITE_S3_REGION=ap-south-1
```

### Explanation:
1. **`VITE_API_URL`**: The base URL for your backend API.
2. **`VITE_S3_BUCKET_NAME`**: The name of your AWS S3 bucket.
3. **`VITE_S3_REGION`**: The AWS region where your S3 bucket is located.

These variables can be accessed in your React application using `import.meta.env`. For example:
```javascript
const apiUrl = import.meta.env.VITE_API_URL;
console.log(apiUrl);